#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: pubsubpluginREDISCLUSTER

set( HEADER_FILES 
  ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
  ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
  ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  include/crc16.h
  include/pubsubpluginREDISCLUSTER.h
  include/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClient.h
  include/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientConfig.h
  include/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientTopic.h
  include/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientTopicConfig.h
  include/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientTopicReader.h
  include/pubsubpluginREDISCLUSTER_RedisNode.h
  include/pubsubpluginREDISCLUSTER_config.h
  include/pubsubpluginREDISCLUSTER_macros.h
)

set( SOURCE_FILES 
  ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
  ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
  ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  src/crc16.cpp
  src/pubsubpluginREDISCLUSTER.cpp
  src/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClient.cpp
  src/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientConfig.cpp
  src/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientTopic.cpp
  src/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientTopicConfig.cpp
  src/pubsubpluginREDISCLUSTER_CRedisClusterPubSubClientTopicReader.cpp
  src/pubsubpluginREDISCLUSTER_RedisNode.cpp
)



if (ANDROID32)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "ANDROID32" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "ANDROID32" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (ANDROID64)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "ANDROID64" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "ANDROID64" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (ARDUINO)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "ARDUINO" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "ARDUINO" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (EMSCRIPTEN32)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "EMSCRIPTEN32" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "EMSCRIPTEN32" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (EMSCRIPTEN64)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "EMSCRIPTEN64" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "EMSCRIPTEN64" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (GLX)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "GLX" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "GLX" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (GTK)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "GTK" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "GTK" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (IOS)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "IOS" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "IOS" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (LINUX32)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "LINUX32" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "LINUX32" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (LINUX64)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "LINUX64" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "LINUX64" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (NACL)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "NACL" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "NACL" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (OSX)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "OSX" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "OSX" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (POSIX)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "POSIX" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "POSIX" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (SDL)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "SDL" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "SDL" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (SYMBIAN)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "SYMBIAN" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "SYMBIAN" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (UNIX)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "UNIX" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "UNIX" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (WIN32)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "WIN32" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "WIN32" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

elseif (WIN64)
  set( PLATFORM_HEADER_FILES 
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCache.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyCacheUpdateTask.h
    ../../../tools/redisinfo/include/redisinfo_CRedisClusterKeyPrunerTask.h
  )

  set( PLATFORM_HEADER_INSTALL "WIN64" )
  source_group( "Platform Header Files" FILES ${PLATFORM_HEADER_FILES} )

  set( PLATFORM_SOURCE_FILES 
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCache.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyCacheUpdateTask.cpp
    ../../../tools/redisinfo/src/redisinfo_CRedisClusterKeyPrunerTask.cpp
  )

  set( PLATFORM_SOURCE_INSTALL "WIN64" )
  source_group( "Platform Source Files" FILES ${PLATFORM_SOURCE_FILES} )

endif ()

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )

set( MODULE_NAME "pubsubpluginREDISCLUSTER" )
add_library( ${MODULE_NAME} ${ALL_FILES} )
add_dependencies( ${MODULE_NAME} gucefCOMCORE gucefCORE gucefMT gucefPUBSUB hiredis redis-plus-plus )
target_link_libraries( ${MODULE_NAME} gucefCOMCORE gucefCORE gucefMT gucefPUBSUB hiredis redis-plus-plus )
set_property( TARGET ${MODULE_NAME} APPEND_STRING PROPERTY COMPILE_DEFINITIONS "MODULE_CONSENSUS_NAME=pubsubpluginREDISCLUSTER;MODULE_NAME=pubsubpluginREDISCLUSTER;PUBSUBPLUGIN_REDISCLUSTER_BUILD_PLUGIN_DLL" )

include_directories( .. ../.. ../../.. ../../../common/include ../../../dependencies/ ../../../dependencies/hiredis ../../../dependencies/redis-plus-plus/src/ ../../../dependencies/redis-plus-plus/src/sw/redis++ ../../../dependencies/redis-plus-plus/src/sw/redis++/cxx11 ../../../dependencies/redis-plus-plus/src/sw/redis++/cxx11/sw ../../../dependencies/redis-plus-plus/src/sw/redis++/cxx11/sw/redis++ ../../../dependencies/redis-plus-plus/src/sw/redis++/no_tls ../../../dependencies/redis-plus-plus/src/sw/redis++/no_tls/sw ../../../dependencies/redis-plus-plus/src/sw/redis++/no_tls/sw/redis++ ../../../dependencies/redis-plus-plus/src/sw/redis++/patterns ../../../platform/gucefCOM/include ../../../platform/gucefCOMCORE/include ../../../platform/gucefCORE/include ../../../platform/gucefCORE/include/c_api ../../../platform/gucefMT/include ../../../platform/gucefPUBSUB/include ../../../platform/gucefVFS/include ../../../platform/gucefWEB/include ../../../tools ../../../tools/redisinfo ../../../tools/redisinfo/include include )

if ( ANDROID32 )
  include_directories( ../../../dependencies/hiredis ../../../platform/gucefCORE/include/android ../../../tools/redisinfo/include/ )
endif( ANDROID32 )

if ( ANDROID64 )
  include_directories( ../../../dependencies/hiredis ../../../platform/gucefCORE/include/android ../../../tools/redisinfo/include/ )
endif( ANDROID64 )

if ( ARDUINO )
  include_directories( ../../../dependencies/hiredis ../../../tools/redisinfo/include/ )
endif( ARDUINO )

if ( EMSCRIPTEN32 )
  include_directories( ../../../dependencies/hiredis ../../../tools/redisinfo/include/ )
endif( EMSCRIPTEN32 )

if ( EMSCRIPTEN64 )
  include_directories( ../../../dependencies/hiredis ../../../tools/redisinfo/include/ )
endif( EMSCRIPTEN64 )

if ( GLX )
  include_directories( ../../../tools/redisinfo/include/ )
endif( GLX )

if ( GTK )
  include_directories( ../../../tools/redisinfo/include/ )
endif( GTK )

if ( IOS )
  include_directories( ../../../dependencies/hiredis ../../../tools/redisinfo/include/ )
endif( IOS )

if ( LINUX32 )
  include_directories( ../../../dependencies/hiredis ../../../platform/gucefCOMCORE/include/linux ../../../platform/gucefCORE/include/linux ../../../tools/redisinfo/include/ )
endif( LINUX32 )

if ( LINUX64 )
  include_directories( ../../../dependencies/hiredis ../../../platform/gucefCOMCORE/include/linux ../../../platform/gucefCORE/include/linux ../../../tools/redisinfo/include/ )
endif( LINUX64 )

if ( NACL )
  include_directories( ../../../tools/redisinfo/include/ )
endif( NACL )

if ( OSX )
  include_directories( ../../../dependencies/hiredis ../../../tools/redisinfo/include/ )
endif( OSX )

if ( POSIX )
  include_directories( ../../../tools/redisinfo/include/ )
endif( POSIX )

if ( SDL )
  include_directories( ../../../tools/redisinfo/include/ )
endif( SDL )

if ( SYMBIAN )
  include_directories( ../../../tools/redisinfo/include/ )
endif( SYMBIAN )

if ( UNIX )
  include_directories( ../../../tools/redisinfo/include/ )
endif( UNIX )

if ( WIN32 )
  include_directories( ../../../dependencies/hiredis ../../../platform/gucefCOMCORE/include/mswin ../../../platform/gucefCORE/include/mswin ../../../platform/gucefMT/include/mswin ../../../tools/redisinfo/include/ )
endif( WIN32 )

if ( WIN64 )
  include_directories( ../../../dependencies/hiredis ../../../platform/gucefCOMCORE/include/mswin ../../../platform/gucefCORE/include/mswin ../../../platform/gucefMT/include/mswin ../../../tools/redisinfo/include/ )
endif( WIN64 )



#******** START OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


GUCEF_config_lib( ${MODULE_NAME} )


#******** END OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


