#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: dstorepluginYAML

set( HEADER_FILES 
  include/DLLMainDSTOREpluginYAML.h
)

set( SOURCE_FILES 
  src/DLLMainDSTOREpluginYAML.c
)

# Make sure the PLATFORM_HEADER_FILES variable is always defined
 set( PLATFORM_HEADER_FILES "" )

# Make sure the PLATFORM_SOURCE_FILES variable is always defined
 set( PLATFORM_SOURCE_FILES "" )

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )

set( MODULE_NAME "dstorepluginYAML" )
if( EMSCRIPTEN32 )
  add_library( ${MODULE_NAME} STATIC ${ALL_FILES} )
elseif( EMSCRIPTEN64 )
  add_library( ${MODULE_NAME} STATIC ${ALL_FILES} )
else()
  add_library( ${MODULE_NAME} ${ALL_FILES} )
endif()
add_dependencies( ${MODULE_NAME} gucefCORE gucefMT yaml )
target_link_libraries( ${MODULE_NAME} yaml )
set_property( TARGET ${MODULE_NAME} APPEND_STRING PROPERTY COMPILE_DEFINITIONS "DSTOREPLUGINYAML_BUILD_MODULE;MODULE_CONSENSUS_NAME=dstorepluginYAML;MODULE_IS_PLUGIN=1;MODULE_NAME=dstorepluginYAML" )

if ( EMSCRIPTEN32 )
  set_property( TARGET ${MODULE_NAME} APPEND_STRING PROPERTY COMPILE_DEFINITIONS "MODULE_API_PREFIX=dstorepluginYAML" )
endif( EMSCRIPTEN32 )

if ( EMSCRIPTEN64 )
  set_property( TARGET ${MODULE_NAME} APPEND_STRING PROPERTY COMPILE_DEFINITIONS "MODULE_API_PREFIX=dstorepluginYAML" )
endif( EMSCRIPTEN64 )

include_directories( ../../../common/include ../../../dependencies/libyaml/include ../../../dependencies/libyaml/src ../../../platform/gucefCORE/include ../../../platform/gucefCORE/include/c_api ../../../platform/gucefMT/include include )

if ( ANDROID32 )
  include_directories( ../../../platform/gucefCORE/include/android )
endif( ANDROID32 )

if ( ANDROID64 )
  include_directories( ../../../platform/gucefCORE/include/android )
endif( ANDROID64 )

if ( LINUX32 )
  include_directories( ../../../platform/gucefCORE/include/linux )
endif( LINUX32 )

if ( LINUX64 )
  include_directories( ../../../platform/gucefCORE/include/linux )
endif( LINUX64 )

if ( WIN32 )
  include_directories( ../../../platform/gucefCORE/include/mswin ../../../platform/gucefMT/include/mswin )
endif( WIN32 )

if ( WIN64 )
  include_directories( ../../../platform/gucefCORE/include/mswin ../../../platform/gucefMT/include/mswin )
endif( WIN64 )



#******** START OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


GUCEF_config_lib( ${MODULE_NAME} )


#******** END OF AUTOGENERATED TEMPLATED ADDITION CONTENT ********


