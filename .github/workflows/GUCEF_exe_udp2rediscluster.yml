name: target_GUCEF_exe_udp2rediscluster

on:
  workflow_dispatch:
    inputs:
      archiveBuildArtifacts:
        description: 'Archive Build Artifacts?'
        required: false
        default: 'false'
      targetPlatform:
        description: 'Target platform to build?'
        required: false
        default: 'all'

jobs:

  GUCEF_exe_udp2rediscluster-all:
    runs-on: self-hosted
    env:
      TARGET_NAME: GUCEF_exe_udp2rediscluster
      TARGET_PLATFORM: all
      TARGET_PROJECT: projects/CMake/targets/GUCEF_exe_udp2rediscluster
      PRODUCT_NAME: udp2rediscluster
      CI_TOOL: GITHUB-ACTIONS
      BUILD_TOOL: CMAKE
    steps:
      - uses: actions/checkout@master
      - name: Build udp2rediscluster for platform all
        run: ./projects/CI/targets/ci_build.sh
      - name: Archive Debian build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: Debian packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-all/packages/*udp2rediscluster*.deb
      - name: Archive RPM build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: RPM packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-all/packages/*udp2rediscluster*.rpm
      - name: Archive Tarball - bz2 compressed build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: tar.bz2 packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-all/packages/*udp2rediscluster*.tar.bz2
      - name: Archive Tarball - Xz compressed build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: tar.xz packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-all/packages/*udp2rediscluster*.tar.xz
  GUCEF_exe_udp2rediscluster-linux32:
    runs-on: self-hosted
    env:
      TARGET_NAME: GUCEF_exe_udp2rediscluster
      TARGET_PLATFORM: linux32
      TARGET_PROJECT: projects/CMake/targets/GUCEF_exe_udp2rediscluster
      PRODUCT_NAME: udp2rediscluster
      CI_TOOL: GITHUB-ACTIONS
      BUILD_TOOL: CMAKE
    steps:
      - uses: actions/checkout@master
      - name: Build udp2rediscluster for platform linux32
        run: ./projects/CI/targets/ci_build.sh
      - name: Archive Debian build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: Debian packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux32/packages/*udp2rediscluster*.deb
      - name: Archive RPM build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: RPM packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux32/packages/*udp2rediscluster*.rpm
      - name: Archive Tarball - bz2 compressed build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: tar.bz2 packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux32/packages/*udp2rediscluster*.tar.bz2
      - name: Archive Tarball - Xz compressed build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: tar.xz packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux32/packages/*udp2rediscluster*.tar.xz
  GUCEF_exe_udp2rediscluster-linux64:
    runs-on: self-hosted
    env:
      TARGET_NAME: GUCEF_exe_udp2rediscluster
      TARGET_PLATFORM: linux64
      TARGET_PROJECT: projects/CMake/targets/GUCEF_exe_udp2rediscluster
      PRODUCT_NAME: udp2rediscluster
      CI_TOOL: GITHUB-ACTIONS
      BUILD_TOOL: CMAKE
    steps:
      - uses: actions/checkout@master
      - name: Build udp2rediscluster for platform linux64
        run: ./projects/CI/targets/ci_build.sh
      - name: Archive Debian build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: Debian packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux64/packages/*udp2rediscluster*.deb
      - name: Archive RPM build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: RPM packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux64/packages/*udp2rediscluster*.rpm
      - name: Archive Tarball - bz2 compressed build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: tar.bz2 packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux64/packages/*udp2rediscluster*.tar.bz2
      - name: Archive Tarball - Xz compressed build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: tar.xz packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-linux64/packages/*udp2rediscluster*.tar.xz
  GUCEF_exe_udp2rediscluster-win32:
    runs-on: self-hosted
    env:
      TARGET_NAME: GUCEF_exe_udp2rediscluster
      TARGET_PLATFORM: win32
      TARGET_PROJECT: projects\CMake\targets\GUCEF_exe_udp2rediscluster
      PRODUCT_NAME: udp2rediscluster
      CI_TOOL: GITHUB-ACTIONS
      BUILD_TOOL: CMAKE
    steps:
      - uses: actions/checkout@master
      - name: Build udp2rediscluster for platform win32
        run: .\projects\CI\targets\ci_build.ps1
      - name: Archive ZIP build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: ZIP packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-win32/packages/*udp2rediscluster*.zip
  GUCEF_exe_udp2rediscluster-win64:
    runs-on: self-hosted
    env:
      TARGET_NAME: GUCEF_exe_udp2rediscluster
      TARGET_PLATFORM: win64
      TARGET_PROJECT: projects\CMake\targets\GUCEF_exe_udp2rediscluster
      PRODUCT_NAME: udp2rediscluster
      CI_TOOL: GITHUB-ACTIONS
      BUILD_TOOL: CMAKE
    steps:
      - uses: actions/checkout@master
      - name: Build udp2rediscluster for platform win64
        run: .\projects\CI\targets\ci_build.ps1
      - name: Archive ZIP build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: ZIP packages
          path: |
            ./common/bin/GUCEF_exe_udp2rediscluster-win64/packages/*udp2rediscluster*.zip
